"""The dashboard UI. The only package that touches Tkinter.

app.py owns the window, the thumbnail handles and the refresh tick; overview.py
and detail.py are the two views it switches between; theme.py holds the palette.
"""
